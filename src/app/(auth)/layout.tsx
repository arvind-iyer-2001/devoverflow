import React from "react";

const AuthLayout = ({ children }: { children: React.ReactNode }) => {
  return <div className="align-center justify-center ">{children}</div>;
};

export default AuthLayout;
