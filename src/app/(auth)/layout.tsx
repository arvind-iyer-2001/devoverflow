import React from "react";

const AuthLayout = ({ children }: { children: React.ReactNode }) => {
  return <div className="justify-center align-middle ">{children}</div>;
};

export default AuthLayout;
